<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:xi="http://www.w3.org/2001/XInclude"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-3.2.xsd">
	<!-- Konfiguracja Spring Security -->
	
	<http auto-config="true" use-expressions="true">
		
		<intercept-url pattern="/list" access="hasRole('ADMIN')" />
		<intercept-url pattern="/register.**" access="isAuthenticated()" />
		<intercept-url pattern="/register/" access="isAuthenticated()" />
		<intercept-url pattern="/register/*" access="isAuthenticated()" />
		
		
		<form-login login-page="/login" default-target-url="/"
			authentication-failure-url="/login-er1ror" username-parameter="pesel"
			password-parameter="password" />
		<logout logout-success-url="/logout" />
		<access-denied-handler error-page="/accessDenied/" />
	</http>

	<authentication-manager>
	  <authentication-provider>
<!-- 	  	<password-encoder hash="sha-256" /> -->
		<jdbc-user-service data-source-ref="dataSource"
		  users-by-username-query=
		    "SELECT pesel, password, 1 as enabled FROM USER WHERE pesel = ?"
		  authorities-by-username-query= "SELECT r.name, u.pesel FROM ROLE r, USER u WHERE u.IDR = r.ID AND u.PESEL = ?"/>
	  </authentication-provider>
	</authentication-manager>

   <beans:bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
    <beans:property name="driverClassName" value="org.h2.Driver" />
    <beans:property name="url" value="jdbc:h2:tcp://localhost/~/Next1" />
    <beans:property name="username" value="sa" />
    <beans:property name="password" value="sa" />
	</beans:bean>

</beans:beans>